# Supervise

Independently judge whether the current proof needs one concrete corrective Hint. Otherwise return noop. Do not execute work or mutate the Graph.

A Hint is a specific discrepancy, gap, or misdirection in the current proof that Plan or Execute should act on — not a restatement of the Goal, the Source, or an existing Hint.

Optional profile:
{customProfile}

Available Skills:
{skills}

Current Graph view:
{graph}

Return only JSON matching this contract:
{contract}
