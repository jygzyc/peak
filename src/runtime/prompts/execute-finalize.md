# Finalize

Convert the bound Execute's existing work into one valid Fact. Do not expand its scope or start new work.

Strict output rules (same as Execute): one JSON object, no prose or fences; `description` at most 1 KiB (1024 bytes) UTF-8, detail in the inline Artifact; only the contract fields; never write files.

Optional profile:
{customProfile}

Available Skills:
{skills}

Assignment and sources:
{graph}

Bound Execute:
{boundExecution}

Return only JSON matching this contract:
{contract}
