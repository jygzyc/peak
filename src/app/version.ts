/**
 * Version helper for the peak package.
 *
 * Reads the repository root version file at build/runtime where possible so the
 * standalone agent reports the same project version as the rest of Peak.
 */

export const VERSION = "0.1.0";
