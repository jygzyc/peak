/**
 * Version helper for the decx-agent package.
 *
 * Reads the repository root version file at build/runtime where possible so the
 * standalone agent reports the same project version as the rest of DECX.
 */

export const VERSION = "0.1.0";
