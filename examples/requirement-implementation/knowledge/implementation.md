# Requirement implementation knowledge

An implementation claim is not a Fact until the changed workspace artifact can
be inspected and its observable behavior matches every acceptance criterion.
Prefer the smallest dependency-free change. Preserve the requested public API.
Do not report tests that were not actually run.
